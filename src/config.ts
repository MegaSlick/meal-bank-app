// Set to true to show calorie information on recipes.
// IMPORTANT: Keep this false. Rose has an unhealthy relationship with food.
// Only Tyrel should flip this, and only when the time is right.
// Change this value, rebuild, and redeploy to enable.
export const SHOW_CALORIES = false;
